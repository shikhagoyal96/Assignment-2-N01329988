@model  List<PetGrooming.Models.Species>
@{
    ViewBag.Title = "Add";
}

<h2>Add a New Pet</h2>
<!--Adding a new pet to the database-->
<form method="POST" action="/Pet/Add">
    
    <div><span>Name:</span><input id="PetName" type="text" name="PetName" /></div>
    <div>
        <span>Species:</span>
        <select id="SpeciesID" type="text" name="SpeciesID"><!--dropdown to choose a particylar specie for the pet-->
            @{ 
                
                foreach(var species in Model)
                {
                    <option value="@species.SpeciesID">@species.Name</option>

                }
            }
        </select>
    </div>
    <div><span>Weight:</span><input id="PetWeight" type="text" name="PetWeight" />KG</div>
    <div><span>Color:</span><input id="PetColor" type="text" name="PetColor" /></div>
    <div>Notes:</div>
    <textarea id="PetNotes" name="PetNotes">

    </textarea>
    <input type="submit" value="Add Pet">
</form>
